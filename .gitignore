# Dependencies
node_modules/
venv/

# Environment
.env
.env.local
.env.dev
.env.*
!.env.example

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Build
dist/
build/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Logs
server.log
*.log

# Databases
runtime/usage.db
runtime/usage.db-shm
runtime/usage.db-wal
*.db-shm
*.db-wal

# Device identity (auto-generated on first run — never commit)
.device-identity.json

# Sensitive / private
runtime/known_faces/*
!runtime/known_faces/.gitkeep
runtime/faces/*/*
agent_config_backup.json
docs/agent-snapshot-*.json
TOOLS.md
!setup/openvoiceui-agent/TOOLS.md
prompts/dj_prompt_*.txt
prompts/hume_config_snapshot*.txt

# Claude Code project instructions and local dev notes — never commit these
CLAUDE.md
development-notes/

# Netlify
.netlify/

# Misc generated
memory_docs.json
face_owners.json
job_runner.log
*.pkl
refactor-state/

# Runtime / private
runtime/.active-profile
runtime/.voice-session-counter
face_owners.json
agent_config_backup.json
runtime/generated_music/*
runtime/music/*.mp3
runtime/canvas-pages/*.html
runtime/canvas-pages/*.bak
runtime/uploads/*
runtime/transcripts/*
!runtime/uploads/.gitkeep
!runtime/transcripts/.gitkeep
!runtime/generated_music/.gitkeep
!runtime/canvas-pages/.gitkeep
!runtime/music/.gitkeep
runtime/canvas-manifest.json
runtime/canvas-manifest.bak

# Plugins — installed locally, not committed to the base repo.
# Each plugin is its own repo: git clone <url> plugins/<id>
# The example-gateway reference and README are tracked.
plugins/*/
!plugins/example-gateway/
!plugins/README.md
!plugins/.gitkeep
.coverage
.pytest_cache/
backups/
docs/session-*.md

# Personal/server-specific session notes
setup-notes-*.md
setup-base-sudo.sh
*.bak

# Dev/test files (not for public repo)
stt-test.html
index-pi.html
base.html
test_supertonic.py
tools_health_check.py
job_runner.sh
pi_notes/

# Root-level runtime data (canonical location is runtime/)
.active-profile
.voice-session-counter
canvas-manifest.json
usage.db
/faces/
/known_faces/
/generated_music/
/transcripts/
/uploads/

# Local reference docs
OpenVoiceUi-system-prompt-tags

# Local admin scripts (never commit — use ~/scripts/ or project scripts/ for local-only ops)
scripts/

# Legacy doc locations (moved to docs/)
README_SUPERTONIC.md
SUPERTONIC_SETUP.md
INTEGRATION_DETAILS.md
TTS_PROVIDERS.md
